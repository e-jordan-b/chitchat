import { useLoaderData } from 'react-router-dom'

export default function Dashboard() {
  return (
    <div className='border-2 border-red-400 w-full h-full flex flex-col justify-center items-center'>
      <div>Dashboard</div>
    </div>
  )
}
